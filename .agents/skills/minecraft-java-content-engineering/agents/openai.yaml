interface:
  display_name: "Minecraft Java Content Engineering"
  short_description: "Build and validate Java 1.21.8 datapacks and resource packs"
  default_prompt: "Use $minecraft-java-content-engineering to route the task to the bundled datapack or resource-pack workflow, loading only the references needed for that content type."
policy:
  allow_implicit_invocation: true
